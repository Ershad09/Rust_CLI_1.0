
# Echo CLI in Rust

This project reads arguments from the terminal and prints them back as output, similar to the Linux echo command.

   **Example** 

   ```
   cargo run -- Hello Rust

   ```

   Output: ```  Hello Rust ```  

  
